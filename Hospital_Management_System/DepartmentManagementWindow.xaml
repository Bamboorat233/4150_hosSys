<Window x:Class="Hospital_MamSys_GUI.DepartmentManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Department Management" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">
    
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Department Management" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="Manage hospital departments and locations" FontSize="13" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtCount" Text="Total: 0" FontSize="14" Foreground="#7F8C8D" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Button Content="🔄 Refresh" Style="{StaticResource ButtonStyle}" Click="BtnRefresh_Click" Margin="0,0,10,0"/>
                    <Button Content="➕ Add New" Style="{StaticResource ButtonStyle}" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Data Grid -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                </Border.Effect>
                <DataGrid x:Name="dgDepartments" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          RowHeight="40"
                          FontSize="13"
                          BorderThickness="0"
                          Background="White"
                          SelectionChanged="DgDepartments_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding DepartmentID}" Width="80"/>
                        <DataGridTextColumn Header="Department Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="*"/>
                    </DataGrid.Columns>
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#ECF0F1"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#BDC3C7"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#EBF5FB"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#D6EAF8"/>
                                    <Setter Property="Foreground" Value="#2C3E50"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Border>

            <!-- Form Panel -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                </Border.Effect>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Department Details" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                        <!-- Department ID -->
                        <TextBlock Text="Department ID:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtDepartmentID" IsReadOnly="True" Background="#ECF0F1" Margin="0,0,0,10"/>

                        <!-- Name -->
                        <TextBlock Text="Department Name:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtName" Margin="0,0,0,10"/>

                        <!-- Location -->
                        <TextBlock Text="Location:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtLocation" Margin="0,0,0,20"/>

                        <!-- Action Buttons -->
                        <Button Content="💾 Save" Style="{StaticResource ButtonStyle}" Click="BtnSave_Click" Margin="0,0,0,10"/>
                        <Button Content="🗑️ Delete" Style="{StaticResource ButtonStyle}" Click="BtnDelete_Click" Margin="0,0,0,10"/>
                        <Button Content="Clear Form" Style="{StaticResource ButtonStyle}" Click="BtnClear_Click" Margin="0,0,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" Margin="0,15,0,0">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
            </Border.Effect>
            <Grid>
                <TextBlock Text="💡 Tip: Select a department from the list to view or edit details" 
                          FontSize="12" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                <Button Content="✖ Close" Style="{StaticResource ButtonStyle}" 
                       HorizontalAlignment="Right" Click="BtnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>

