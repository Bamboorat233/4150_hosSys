<Window x:Class="Hospital_MamSys_GUI.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Management" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">
    
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="Manage system users and access control" FontSize="13" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtCount" Text="Total: 0" FontSize="14" Foreground="#7F8C8D" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Button Content="🔄 Refresh" Style="{StaticResource ButtonStyle}" Click="BtnRefresh_Click" Margin="0,0,10,0"/>
                    <Button Content="➕ Add New" Style="{StaticResource ButtonStyle}" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Data Grid -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                </Border.Effect>
                <DataGrid x:Name="dgUsers" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          RowHeight="40"
                          FontSize="13"
                          BorderThickness="0"
                          Background="White"
                          SelectionChanged="DgUsers_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding UserID}" Width="60"/>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                        <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat='yyyy-MM-dd'}" Width="120"/>
                    </DataGrid.Columns>
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#ECF0F1"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#BDC3C7"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#EBF5FB"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#D6EAF8"/>
                                    <Setter Property="Foreground" Value="#2C3E50"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Border>

            <!-- Form Panel -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
                </Border.Effect>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="User Details" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                        <!-- User ID -->
                        <TextBlock Text="User ID:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtUserID" IsReadOnly="True" Background="#ECF0F1" Margin="0,0,0,10"/>

                        <!-- Username -->
                        <TextBlock Text="Username:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtUsername" Margin="0,0,0,10"/>

                        <!-- Password -->
                        <TextBlock Text="Password:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="txtPassword" Margin="0,0,0,10"/>

                        <!-- Role -->
                        <TextBlock Text="Role:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbRole" Margin="0,0,0,10">
                            <ComboBoxItem Content="Admin"/>
                            <ComboBoxItem Content="Doctor"/>
                            <ComboBoxItem Content="Nurse"/>
                            <ComboBoxItem Content="Receptionist"/>
                        </ComboBox>

                        <!-- Email -->
                        <TextBlock Text="Email:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtEmail" Margin="0,0,0,10"/>

                        <!-- Is Active -->
                        <CheckBox x:Name="chkIsActive" Content="Is Active" FontSize="13" Margin="0,0,0,20"/>

                        <!-- Action Buttons -->
                        <Button Content="💾 Save" Style="{StaticResource ButtonStyle}" Click="BtnSave_Click" Margin="0,0,0,10"/>
                        <Button Content="🗑️ Delete" Style="{StaticResource ButtonStyle}" Click="BtnDelete_Click" Margin="0,0,0,10"/>
                        <Button Content="Clear Form" Style="{StaticResource ButtonStyle}" Click="BtnClear_Click" Margin="0,0,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15" Margin="0,15,0,0">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" Opacity="0.3" BlurRadius="8"/>
            </Border.Effect>
            <Grid>
                <TextBlock Text="💡 Tip: Select a user from the list to view or edit details. Password is required for new users." 
                          FontSize="12" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                <Button Content="✖ Close" Style="{StaticResource ButtonStyle}" 
                       HorizontalAlignment="Right" Click="BtnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>

